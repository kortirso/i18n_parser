defmodule I18nParser.Convert.Json do
  @moduledoc """
  Module for reading data from Json file
  """

  defmacro __using__(_opts) do
    quote do
      defp do_convert_json(file) do
      end
    end
  end
end
